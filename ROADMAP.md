# 轻阅 Markdown 后续计划

本文件记录已经讨论清楚、但尚未进入当前稳定版本的功能。具体版本号以发布时为准。

## 下一版本候选

### 1. 分栏滚动同步模式

在分栏模式提供清楚、可切换的同步状态：

- **关闭同步**：编辑区和预览区各自滚动，互不影响。
- **自动锚点**：根据相同标题或邻近内容定位，不使用整篇文档高度百分比硬套位置。
- **手动校准后继续同步**：用户在预览区主动调整位置后，将这次相对位置保存为新的同步基准；随后滚动编辑区时继续保持该位置差，不得跳回程序原先计算的位置。

同时提供“重新对齐”操作，让用户可以主动清除手动校准并恢复自动锚点。

需要覆盖的真实场景：编辑区的 `1.4` 位于窗口下方，而预览区的 `1.4` 被用户手动调到窗口上方；再次滚动编辑区后，预览区仍应保持用户校准后的相对位置。切换文档、模式或主动点击“重新对齐”时，才清除校准。

### 2. 空格与空白行快捷复制

语法速查应直接显示可手动选取的原始写法，并提供只复制符号的按钮：

- 段首缩进：`&emsp;&emsp;`
- 换一行：`<br>`
- 空出一整行：`<br><br>`
- 两个中文全角空格：`　　`（作为不依赖 HTML 实体的替代方案）

复制结果不得附带“正文从这里开始”“上一段文字”“下一段文字”等示例内容。

说明中需要明确：`&emsp;` 和 `<br>` 属于 Markdown 中常见的内嵌 HTML，并不是核心 Markdown 语法；不同平台是否允许内嵌 HTML，取决于该平台的渲染规则。

### 3. 安全的尖括号自动候选

在编辑模式输入 `<` 后显示轻量候选菜单，首批只提供轻阅已经支持且经过安全检查的格式：

- `<br>`
- `<br><br>`
- `<mark>高光文字</mark>`
- `<span class="text-red">红色文字</span>`

交互要求：

- 支持鼠标选择，以及方向键选择、Enter 确认、Esc 关闭。
- 插入后选中需要替换的示例文字，方便直接输入，不要求用户手工删除。
- 不影响编辑模式下方向键移动光标，也不得触发阅读模式翻页。
- 不开放任意标签、脚本、事件属性或危险 URL；渲染仍必须经过 DOMPurify。

## 下一版本验收重点

- 关闭同步后，左右两区滚动完全独立。
- 自动锚点在字体、行高、图片、表格高度不同的长文中仍能定位到相同内容附近。
- 手动校准后，连续上下滚动编辑区不会丢失用户设定的位置差。
- “重新对齐”能够明确恢复自动锚点。
- 语法按钮写入系统剪贴板的内容必须与按钮标示完全一致，不夹带示例文字。
- 自动候选可用键盘完整操作，并通过不受信任 HTML/脚本安全测试。
