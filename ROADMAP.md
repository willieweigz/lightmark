# 轻阅 Markdown 后续计划

本文件记录已落实的讨论结果和仍需后续处理的平台工作。具体发布版本号以 Release 为准。

## Windows 0.5.3 已完成

- 新增可收起、可调宽度的 Codex 助读侧栏，支持整篇文章与选中文字两种上下文。
- 新增“只依据原文 / 自然回答 / 联网查证”三档回答方式；切换档位会开启新对话，避免不同知识边界互相污染。
- 联网查证会实际调用 Codex 网页搜索并列出来源；其他两档禁用网页搜索。
- 所有档位均阻止命令执行、任意本地文件读写、MCP、动态工具和子代理，Markdown 内容始终按不受信任资料处理。
- Windows 编辑区新增 `Alt+1` 插入 `&emsp;&emsp;`、`Alt+2` 插入 `<br><br>`，并保留常用编辑快捷键。
- 已完成前端 18 项、Rust 6 项自动化测试，以及三档真实问答和 Windows 生产构建验收。

## Windows 0.5.2 已完成

- 分栏滚动提供“自动锚点 / 手动校准 / 关闭同步”三种状态；手动校准会持续保留位置差，并可用“重新对齐”清除。
- 语法速查直接显示 `&emsp;&emsp;`、两个中文全角空格、`<br>`、`<br><br>`，复制按钮只写入符号本身，不附带示例文字。
- 编辑模式输入 `<` 会显示受控候选：`<br>`、`<br><br>`、`<mark>高光文字</mark>`、`<span class="text-red">红色文字</span>`；支持鼠标、方向键、Enter、Tab 和 Esc。
- 修复长文档在普通窗口、Windows 最大化及全屏幕时只占顶部一小块的问题。
- 安全边界不变：不开放任意 HTML 候选，所有渲染仍经过 DOMPurify。

上述功能已经用长文档和 Examples 三篇文档完成 Windows WebView2 回归；合并、发布状态以 GitHub Pull Request 和 Releases 为准。

## 后续候选

### macOS 原生界面对齐

Windows 界面位于 `src/`，macOS 原生界面位于 `Sources/LightMark/main.swift`。共用 `Resources/` 会同步 Markdown 渲染能力，但原生界面仍需分别实现和验收。macOS 已对齐正文查找、语法卡片复制，以及 `&emsp;&emsp;` / `<br><br>` 的按钮和快捷键；滚动同步选择器与尖括号候选仍是后续候选。

### 下一期待定事项：macOS 与 Windows 编辑体验继续对齐

- [ ] 在 macOS 分栏模式加入“自动锚点 / 手动校准 / 关闭同步”三档选择，并提供“重新对齐”。
- [ ] 真实验收手动校准在超长文档、混合字号、大图和宽表格中的位置保持与恢复效果。
- [ ] 在 macOS 编辑区加入受控的尖括号候选：`<br>`、`<br><br>`、`<mark>` 和安全的红色文字格式；支持方向键、Enter、Tab、Esc 与鼠标。
- [ ] 评估在 macOS 编辑区直接显示黄色高光和红色文字效果，同时继续保留、编辑原始 Markdown/HTML 标签。
- [ ] 评估是否把 Windows 的纯文本、富文本整篇复制同步到 macOS；Markdown 原文复制继续保持默认且不得被替换。
- [ ] Codex 助读若要进入 macOS，另开独立功能设计与安全验收，不与基础阅读编辑功能捆绑。

上述项目不属于当前 macOS 图片阅读、正文查找与语法卡片交付范围；进入下一期前需分别确认功能位置、交互和验收样例。

### 后续验收重点

- 在包含大量图片、超长表格和混合字号的文档中继续观察自动锚点精度。
- 在 macOS 完成对应功能后，验证其快捷键、系统剪贴板、窗口缩放和未保存保护没有回归。
- 发布新版本时同时更新 Windows 与 macOS 下载包，并在 Release Notes 中写明两端功能差异。
